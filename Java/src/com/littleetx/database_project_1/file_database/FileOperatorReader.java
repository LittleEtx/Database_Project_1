package com.littleetx.database_project_1.file_database;

import java.io.Closeable;

/**
 * An abstract class used as to read files
 */
public abstract class FileOperatorReader implements Iterable<String[]>, AutoCloseable, Closeable {
}
